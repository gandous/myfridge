[package]
edition      = "2024"
name         = "myfridge-migration"
publish      = false
rust-version = "1.85.0"
version      = "0.1.0"

[dependencies]
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread"] }
sea-orm-migration.workspace = true
